spring:
  jpa:
    hibernate:
      # We strictly validate the schema in tests just like in production
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Enables Flyway to run against the Testcontainer before Hibernate validates
  flyway:
    enabled: true
    locations: classpath:db/migration
